export const environment = {
  appUrl: window.location.origin,
  apiUrl: `${window.location.origin}/api/`,
  accountApiUrl: `https://localhost:5003/api/`,
  feedApiUrl: `https://localhost:5001/api/`,
  production: true
};
